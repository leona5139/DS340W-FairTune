# Raw dataset and pretrained-weight cache are not ours to redistribute /
# not needed for grading — never commit these.
/data/
/Papila/
**/checkpoint*.pth
**/*.pt

__pycache__/
*.pyc

# Project-local virtualenv (see WSL_ROCM_SETUP.md / CUDA_SETUP.md) — never
# commit it.
.venv/
