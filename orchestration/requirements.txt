# layer's preprocessing scripts. Install torch/torchvision FIRST, both inside
# the project's .venv:
#   - Primary path: ROCm PyTorch wheels from repo.radeon.com
#     (see ../WSL_ROCM_SETUP.md) — do not let this pull in a CUDA/CPU-only
#     torch build.
#   - Fallback path (native Windows / no ROCm-supported AMD GPU): CUDA-enabled
#     torch/torchvision (pip install torch torchvision --index-url
#     https://download.pytorch.org/whl/cuXXX — see ../CUDA_SETUP.md).
#
# Data provenance: preprocess_papila.py consumes the raw PAPILA release
# (../../Papila/, ClinicalData/ + FundusImages/). preprocess_glaucoma.py
# consumes the raw Harvard-GF release, downloaded directly (no account/token
# needed) from:
#   https://huggingface.co/datasets/harvardairobotics/Harvard-GF/resolve/main/Dataset/dataset.zip
# See INTERSECTIONAL_FAIRNESS_PLAN.md's "Data acquisition" section for how
# that was confirmed and what the extracted files look like.
timm
optuna
loralib
fairlearn
wandb
pyyaml
pandas
numpy
pillow
scikit-learn
openpyxl
matplotlib
seaborn
