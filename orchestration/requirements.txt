# FairTune itself ships no requirements.txt. This is the set of third-party
# packages actually imported across its .py files plus this orchestration
# layer's preprocessing script. Install CUDA-enabled torch/torchvision FIRST
# (pip install torch torchvision --index-url https://download.pytorch.org/whl/cuXXX
# — see ../CUDA_SETUP.md), then this file, both inside the project's .venv.
timm
optuna
loralib
fairlearn
wandb
pyyaml
pandas
scikit-learn
openpyxl
matplotlib
seaborn
